<a href=<%= "/users/#{user.id}" %> class="user_link">
  <img src=<%= user.photo %> alt=<%= user.name %>/>
  <div class="user_info">
    <h2><%= user.name %></h2>
    <p>Number of posts: <%= user.posts_counter %></p>
  </div>
</a>